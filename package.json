{
  "name": "cloud-itonami-dns-resolver",
  "private": true,
  "description": "Bounded, G7-gated active DNS resolution against Tranco's top-1M list, landing in Cloudflare R2 Data Catalog (Iceberg). 90-docs/adr/2608280900-world-scale-dns-domain-collection.edn",
  "scripts": {
    "resolve": "nbb --classpath src scripts/resolve_tick.cljs",
    "resolve:cc": "nbb --classpath src scripts/resolve_tick.cljs --source commoncrawl",
    "refresh:cc": "nbb --classpath src scripts/refresh_cc_domains.cljs",
    "sync": "nbb --classpath src scripts/export_and_sync.cljs",
    "test": "nbb --classpath src test/resolve_tick_test.cljs"
  }
}
